services:
  nica-pay:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nica-pay
    env_file:
      - .env # Reference the .env file
    volumes:
      - .:/app
    working_dir: /app
    command: ["python3", "src/bot.py"]
